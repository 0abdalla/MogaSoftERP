import { SearchArryPipe } from './search-arry.pipe';

describe('SearchArryPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchArryPipe();
    expect(pipe).toBeTruthy();
  });
});
